Binding Context=this; (dans le constructeur de la collection -> BindingContexte=)

ContentPage
	CollectionView ItemSource="{Binding ___}"
		CollectionView.ItemTemplate
			DataTemplate
				Grid
					Label
					Image
					Label
					...
				Grid
			DataTemplate
		CollectionView.ItemTemplate
	CollectionView		
ContentPage

Pour bindings





// Content view 

Comme une content page mais réutilisable 

ContentView 
	Icon (doit etre une bindable property)
	Title
	Count
	Chevron

voir photo !!!!!!!!!!!!!!!!!!

1. Faire les bindableProperty

public static readonly BindableProperty TitleProperty = BindableProperty.create("Title", typeof(string), "ValeurParDéfaut");

wrapper :

public string Title
{
	get=> GetValue(TitleProperty) as string;
	set=> SetValue(TitleProperty, string);
}

Binding :

<ContentView
	x:Name="root">
	...
	<Label 
		Text="{Binding Title, Source={x:Reference root}}"
	/>
	...
</ContentView>








